import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.avaliacaoFormadora',
  appName: 'avaliacaoFormadora',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
